namespace OnlineSalesCore.Const
{
    /// <summary>
    /// Must be in sync with db except for UNKNOWN
    /// </summary>
    public enum RoleEnum
    {
        UNKNOWN,
        CA,
        BDS,
        ASM,
        ADMIN
    }
}
