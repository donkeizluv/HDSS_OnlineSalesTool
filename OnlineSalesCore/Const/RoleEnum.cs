namespace OnlineSalesCore.Const
{
    /// <summary>
    /// Must be in sync with db
    /// </summary>
    public enum RoleEnum
    {
        UNKNOWN,
        CA,
        BDS,
        ASM,
        ADMIN
    }
}
